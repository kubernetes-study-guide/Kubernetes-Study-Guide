apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}
  labels:
    capabilityApiVersions: {{ .Capabilities.APIVersions }}
spec:
  containers:
  - name: {{ .Values.container_name }}
    image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
    ports:
    - containerPort: 80